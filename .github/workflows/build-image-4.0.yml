name: Build & publish 4.0 image

# ⚠ SPT 4.0 IS FROZEN AT 4.0.13. Do not run this to chase a newer SPT — 4.1 is the
# living line (build-image-4.1.yml, separate package). :latest on THIS package stays
# pinned to 4.0.13 permanently, so nobody running 4.0 is jumped to 4.1 by a pull.
# Leave tag_latest OFF unless you are deliberately re-cutting 4.0.13 itself.
#
# The freeze is SPT-level only: Fika and ModSync still update at runtime on 4.0 via
# AUTO_UPDATE_FIKA / AUTO_UPDATE_MODSYNC (the 4.0 ModSync line is still shipping), so
# mod updates never need a rebuild here either.
#
# Builds the SPT 4.0 server image from source for amd64 AND arm64 — each on a
# NATIVE runner (no QEMU) — and publishes one multi-arch tag to GHCR.
#
# To publish: Actions tab → this workflow → "Run workflow" → enter the SPT version.
# Auth is the built-in GITHUB_TOKEN — no PAT, no docker login on any machine.

on:
  workflow_dispatch:
    inputs:
      spt_version:
        description: "SPT version — FROZEN at 4.0.13; only change this to re-cut the same pin"
        required: true
        default: "4.0.13"
      tag_latest:
        description: "Move :latest to this build (leave OFF — 4.0 :latest is pinned to 4.0.13)"
        type: boolean
        default: false

env:
  REGISTRY: ghcr.io
  IMAGE: dildz/spt-fika-server   # GHCR needs lowercase; the configurator's compose pulls this exact name

jobs:
  # Build each arch on its own native runner, push by digest (no tag yet).
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            runner: ubuntu-24.04
            arch: amd64
          - platform: linux/arm64
            runner: ubuntu-24.04-arm   # GitHub's native arm64 runner (free on public repos)
            arch: arm64
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - name: Build & push by digest (${{ matrix.arch }})
        id: build
        uses: docker/build-push-action@v6
        with:
          context: ./image-4.0
          platforms: ${{ matrix.platform }}
          build-args: |
            SPT_MAJOR=4
            SPT_VERSION=${{ inputs.spt_version }}
          outputs: type=image,name=${{ env.REGISTRY }}/${{ env.IMAGE }},push-by-digest=true,name-canonical=true,push=true

      - name: Save digest
        run: |
          digest="${{ steps.build.outputs.digest }}"
          mkdir -p /tmp/digests
          touch "/tmp/digests/${digest#sha256:}"   # no colon in the filename — keeps upload-artifact happy

      - uses: actions/upload-artifact@v4
        with:
          name: digest-${{ matrix.arch }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  # Stitch the two per-arch digests into one multi-arch tag (pure registry metadata).
  merge:
    needs: build
    runs-on: ubuntu-24.04
    permissions:
      packages: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: /tmp/digests
          pattern: digest-*
          merge-multiple: true

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - name: Create & push manifest list
        working-directory: /tmp/digests
        run: |
          tags="-t ${REGISTRY}/${IMAGE}:${{ inputs.spt_version }}"
          [ "${{ inputs.tag_latest }}" = "true" ] && tags="$tags -t ${REGISTRY}/${IMAGE}:latest"
          docker buildx imagetools create $tags \
            $(for d in *; do printf '%s@sha256:%s ' "${REGISTRY}/${IMAGE}" "$d"; done)

      - name: Show published manifest
        run: docker buildx imagetools inspect "${REGISTRY}/${IMAGE}:${{ inputs.spt_version }}"
